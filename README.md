# Experiment Tracker

A Streamlit application for tracking engineering experiments, evidence, and outcomes.

## Run locally

```bash
python scripts/project_config.py run project.install_command
streamlit run src/experiment_tracker/app.py
```

The first slice provides SQLite-backed experiment storage and a dashboard with a create form.
